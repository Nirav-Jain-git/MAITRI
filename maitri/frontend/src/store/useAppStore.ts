// Re-export the main app store
export { useAppStore } from './appStore';

// Additional store exports if needed
export type { 
  EmotionData, 
  WellnessScore, 
  ConversationContext, 
  SystemAlert, 
  UserPreferences, 
  SessionData 
} from '../types';